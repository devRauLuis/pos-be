export interface IClient {
  address: string,
  cardCode: number,
  createAt: string,
  active: boolean,
  email: string,
  phone: string,
  points: number,
  updateAt: Date,
  visits: number,
  id: number
}