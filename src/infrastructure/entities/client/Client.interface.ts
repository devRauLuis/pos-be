export interface IClient {
  address: string,
  cardCode: number,
  createAt: Date,
  active: boolean,
  email: string,
  phone: string,
  points: number,
  updateAt: Date,
  visits: number,
  id: number
}