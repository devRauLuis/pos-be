export interface IClient {
  address: string,
  cardCode: number,
  createdAt: Date,
  active: boolean,
  email: string,
  phone: string,
  points: number,
  updatedAt: Date,
  visits: number,
  id: number;
}